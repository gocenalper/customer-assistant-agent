services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: stockdb
      POSTGRES_USER: stock
      POSTGRES_PASSWORD: ${DB_PASSWORD:-stock}
    ports:
      - "127.0.0.1:5433:5432"
    volumes:
      - stock-data:/var/lib/postgresql/data

volumes:
  stock-data:
